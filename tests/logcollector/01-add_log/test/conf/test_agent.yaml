---
- hosts: Agents
  roles:
    - test-agent
  vars:
    test_name: 01-add_log
    test_dir: "/{{ test_name }}"
    test_file: "test.py"
    test_conf: "test.conf"
    root_dir: "../.."
    required_files_dir: "{{ root_dir }}/required_files"
    test_files_dir: "{{ root_dir }}/test"
    wazuh_managers:
      - address: "{{ hostvars[groups.Managers[0]]['ansible_host'] }}"

