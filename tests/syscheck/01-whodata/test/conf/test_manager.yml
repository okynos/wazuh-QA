---
- hosts: Managers
  roles:
    - test-manager
  vars:
    test_name: whodata
    test_dir: "/{{ test_name }}"
    required_files: ../../requiredfiles
    local_test_dir: ../../test
    num_agents: 8
    run_time: 30
    wazuh_managers:
      - address: "{{ hostvars[groups.Managers[0]]['ansible_host'] }}"
